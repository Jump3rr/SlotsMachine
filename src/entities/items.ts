//const items = ["🍒", "🍇", "🍉", "🍓", "🍏", "🍍", "🍋", "🏆"];
//const items = [elements, elements, elements];
export interface IItems {
  id: number;
  img: string;
}
//export default items;
